# Test fixtures

Files under `tests/fixtures/` are synthetic verifier and disposable plumbing fixtures only. They must never become retained training data, selected checkpoints, Hugging Face artifacts, or reported model results.
